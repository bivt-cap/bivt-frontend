/**
 * Invite to circle styles
 *
 * @version 0.0.1
 * @author Arshdeep Singh (https://github.com/Singh-Arshdeep)
 */

import {StyleSheet} from 'react-native';

const inviteToCircleStyles = StyleSheet.create({
  inviteToCircleForm: {
    marginTop: '10%',
  },
  inviteToCircleButton: {
    marginTop: '10%',
  },
  textFieldError: {
    color: 'red',
    fontSize: 14,
  },
});

export default inviteToCircleStyles;
