import {StyleSheet} from 'react-native';

const signupStyles = StyleSheet.create({
  signupForm: {
    marginTop: '10%',
  },
  signupButton: {
    marginTop: '10%',
  },
});

export default signupStyles;
