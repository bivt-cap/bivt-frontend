<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>

        <!-- Add the following line to set the default status bar color for all the app. -->
        <item name="android:statusBarColor">@color/app_bg</item>
        <!-- Add the following line to set the default status bar text color for all the app 
        to be a light color (false) or a dark color (true) -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- Add the following line to set the default background color for all the app. -->
        <item name="android:windowBackground">@color/app_bg</item>

        <!-- Add color to the Date Picker and Time Picker Modal -->
        <item name="android:datePickerDialogTheme">@style/DialogDateTimePicker.Theme</item>
        <item name="android:timePickerDialogTheme">@style/DialogDateTimePicker.Theme</item>
    </style>

    <!-- Adds the splash screen definition -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/splashscreen_bg</item>
        <item name="android:background">@drawable/background_splash</item>
    </style>
    
    <!-- Adds the Date Time Picker definition -->
    <style name="DialogDateTimePicker.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#CA60E3</item>
    </style>
</resources>