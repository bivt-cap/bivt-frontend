const chatInitial = {
  loading: false,
  messageDetails: '',
  userDetails: '',
  isLoggedin: 'False',
  error: '',
};

let chatReducer = (state = chatInitial, action) => {};

export default chatReducer;
